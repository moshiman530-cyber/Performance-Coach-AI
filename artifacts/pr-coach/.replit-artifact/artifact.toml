kind = "web"
previewPath = "/"
title = "PR Coach"
version = "1.0.0"
id = "artifacts/pr-coach"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 25555

[services.development]
run = "pnpm --filter @workspace/pr-coach run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/pr-coach", "run", "build" ]
publicDir = "artifacts/pr-coach/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "25555"
BASE_PATH = "/"
